fun hello(n) = (

                                )

                                     fun main() = (
                                          a := newarray (10);
                                          for (i := 0 to 25)(
                                          (dummy := write (a,0,i+65);)
                                          (dummy := write(a,1,10);)
                                          (printstring (a);)
                                          i := i+1;
                                          )
                                )